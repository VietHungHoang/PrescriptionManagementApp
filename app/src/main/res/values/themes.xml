<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PrescriptionManagementApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

    </style>
    <style name="fullCornerImage">
        <item name="cornerSize">50%</item>
        <item name="cornerFamily">rounded</item>
    </style>

    <style name="dropdownBackground">
        <item name="android:background">@drawable/edittext_bg_normal</item>
        <item name="android:drawableEnd">@drawable/ic_dropdown</item>
    </style>

    <style name="Theme.PrescriptionManagementApp" parent="Base.Theme.PrescriptionManagementApp" />

</resources>